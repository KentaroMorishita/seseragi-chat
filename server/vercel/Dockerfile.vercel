FROM oven/bun:1.3.9

WORKDIR /app
COPY server-dist.tgz /tmp/server-dist.tgz
RUN tar -xzf /tmp/server-dist.tgz -C /app \
  && rm /tmp/server-dist.tgz \
  && test -f /app/entry.ts \
  && test -f /app/node_modules/seseragi/runtime-bun/websocket-server.ts

CMD ["bun", "entry.ts"]
